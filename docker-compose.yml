# admin/docker-compose.yml
version: '3.8'

services:
  admin:
    build: .
    ports:
      - "23003:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
